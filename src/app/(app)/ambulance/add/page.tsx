import Addambulanceform from "@/components/ambulances/Addambulanceform";

export default function AddAmbulancePage() {
    return (
        <main className="mx-auto max-w-6xl px-4 py-8 sm:px-6">
            <Addambulanceform />
        </main>
    );
}